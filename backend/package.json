{
  "name": "freelisten-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for FreeListen: resolves a song via yt-dlp's own YouTube search, converts it to a tagged audio file, and saves it to disk.",
  "main": "server.js",
  "scripts": {
    "postinstall": "node scripts/setup-deno.js",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "ffmpeg-static": "^5.2.0",
    "yt-dlp-exec": "^1.0.2"
  }
}
